<Window x:Class="Personas.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Personas"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:ConverterSexo x:Key="convertirSexo"></local:ConverterSexo>
    </Window.Resources>
    <Grid>

        <StackPanel>
            
                <ComboBox x:Name="EjemploLista" ItemsSource="{Binding}" Height="35" Width="150" >
                    <ComboBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:Persona}">
                            <StackPanel Orientation="Horizontal" >
                                <Image Height="50" Width="100" Source="{Binding Path=Foto}"></Image>
                                <TextBlock Text="{Binding Nombre}"></TextBlock>
                                <Image Source="{Binding Sexo, Converter={StaticResource convertirSexo}}"></Image>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            
        </StackPanel>
        
        
        
    </Grid>
</Window>
